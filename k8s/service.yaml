apiVersion: v1
kind: Service
metadata:
  name: k8s-of-empires
  namespace: default

spec:
  type: ClusterIP

  selector:
    app: k8s-of-empires

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080